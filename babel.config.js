// const config = {
//   presets: [['@babel/preset-env', {targets: {node: 'current'}}]],
// };

// export default config;

module.exports = {
  presets: [['@babel/preset-env', {targets: {node: 'current'}}]],
};